#include <iostream>
#include <string.h>
#define N 100010
using namespace std;

unsigned len[N][N];

int main()
{
    int n, m, s, t;
    cin >> n >> m >> s >> t;
    int from, to;
    unsigned length;
    memset(len, -1, sizeof(len));
    for(int i = 1; i <= m; ++i)
    {
        cin >> from >> to >> length;

    }
}
